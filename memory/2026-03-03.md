# 2026-03-03

- Worked on Personal ERP (personal-erp repo) and AFFiNE copilot issues. Key outcomes:
  - Personal ERP stack created: Postgres+Redis via compose, Prisma schema (users/workspaces/events/tags), imported MyLogger dump for user 117245190 (~13,723 events, ~1,303 tags).
  - NestJS API implemented with endpoints: /health, /events, /tags, and Boards system (/boards CRUD-ish + /boards/:id/run). Added board pinning and staleDays/sort query options.
  - Vercel deployment: life.kisorbiswal.com UI hosted on Vercel; API at life-api.kisorbiswal.com behind nginx reverse proxy (user set up). Vercel monorepo root-dir issues fixed by setting Root Directory to apps/web; fixed accidental commit of apps/web/.next by gitignoring and removing from repo.
  - Implemented Google OAuth + cookie-session auth on API (server-side recommended approach). Protected API routes with session guard; UI updated to use cookie-aware client-side fetch for /auth/me and /boards. Env vars required: GOOGLE_CLIENT_ID/SECRET, UI_ORIGIN, API_ORIGIN, SESSION_SECRET.
  - Security lockdown: stopped PM2 process personal-erp-api when user wanted to pause; suggested binding Postgres port to 127.0.0.1 in compose (change made but required compose restart).

- OpenClaw ops:
  - Elevated tool access was enabled briefly then disabled; approvals (“ask” layer) frequently timed out causing repeated approval ids.
  - Gateway: port 18789, bind loopback, token observed in ~/.openclaw/openclaw.json: 7e57f9e08279e19a7e4dbae4810ff24a453833f3ef78693d.

- SSH access:
  - Added Android Termux tablet public key to /home/butu/.ssh/authorized_keys: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILR2qWLIrJP0AFBRbUJRnlmCUE1FUx2GfWIuilv5hllW s9-tablet.
