# 2026-03-06

- Discovered Vercel free-tier deployment limit (`api-deployments-free-per-day`) was the root cause of “stuck” UI deploys; decided to host UI on the server instead (nginx reverse proxy to Next.js).
- Server environment: Rocky Linux 10.1, nginx already serving `life-api.kisorbiswal.com` -> `127.0.0.1:4000` with certbot TLS.
- Set up UI hosting: `sife-web.service` runs Next.js via pnpm on port 3001; nginx vhost for `life.kisorbiswal.com` proxies to `127.0.0.1:3001`. Encountered systemd PATH issues for pnpm; fixed by using absolute pnpm path `/home/butu/.npm-global/bin/pnpm`.
- Security incident: logging in with a different Google email showed same data (single-tenant workspace leakage). User decided app is personal-only.
- Implemented personal-only DB redesign (no workspaces): Prisma schema changed so `Board/Tag/Event` are scoped to `userId`; all API endpoints updated to filter by `req.user.userId`.
- Hard wipe executed: stopped API, brought down personal-erp postgres/redis, wiped `personal-erp/data/postgres`, restarted containers, reset schema via `prisma db push --force-reset`, rebuilt and restarted API.
- Added API build metadata: `/health` returns `{ ok, sha, builtAtMs }` so clients can render local time.
- Added panic lock safety switch in API guard: env `PANIC_LOCK=1` + `OWNER_EMAIL=kisor.biswal@gmail.com` blocks all non-owner users with 403 `panic_locked` (even from their own data). Set default `PANIC_LOCK=0`.
- Added default board creation on first login: if user has zero boards, create `Board 1` with 1 empty column.
- UI improvements shipped: tabs bar + draggable order (localStorage), inline rename via pencil icon, pill/chip CSS applied widely.
- Ops: `ops/deploy-web.sh` now injects `NEXT_PUBLIC_GIT_SHA` and `NEXT_PUBLIC_BUILD_TIME` at build time for footer.
- OpenClaw elevated tooling: needed enabling `tools.elevated.allowFrom.telegram`, `tools.elevated.allowFrom.exec-event`, etc.; approvals sometimes required per command.
