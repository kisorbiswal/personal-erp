---
source: mylogger
mylogger_id: 13808
created: 2018-02-28T05:33:57+00:00
created_raw: 2018-02-28 05:33:57
completed_raw: 2018-02-28 00:34:10
tags:
  - j5
---

epam nn  - Removed the regex hardcoding to config- Done n  - There was no decorator also OCP violation, fix this- Donen  - remove path variable in rest for accepting password - make it post- Donen  - junits are not thorough, just one method, the other for cntroller is unimplemented- Donen  - naming in unit tests is below par.- Done upto best of my current knowledge. I recognize the scope to improve in this area, will definitely do it.n  - see if rules can be loaded dynamically- Donen  - have a core API for services - could be a separate jar- Donen  - increase the unit tests, should be able to check individual rule- Donennnnnn  Projectsn   validation-servicen    Provided core interface for Validator implementors and Validation Servicen   Post contstruction of the validation service, the bean loads all jars available in config dir and creates validator instances from the classes that Implements Validator interface. Also starts a file change listener to listen for jar chages, to support hot reload.n    File change listener: On add new validators are added to validation. On Remove corresponding validators are removed. On Modify remove old and add new validators.n    n    This project shoulbe run "mvn package install" to make the jar available in local repository. Other 3 programs depeds on this.n   n   regex-validator-impln    Validates the given input against the configured regex.n    This jar can be dropped in config folder and the Validator implementations are dynamically loaded.nn   dictionary-validator-impln    Validates the given input against the configured dictionary of wordsn    This jar can be dropped in config folder and the Validator implementations are dynamically loaded.nn   validation-demo-appn    A spring boot application injects the validation-service and uses it.n    If given string is valid it return true and no message, if invalid it returns false and messages containing reason.nnKnown Issuen===========n"^[a-zA-Z0-9]*$",false,"Must consist of a mixture of lowercase letters, and numerical digits only, with at least one of each." nnis not completely correct.nnnnCould not do now due to time constraintn=======================================n Compare checksum of old and new file of modified file, if no change no action should be taken.nnnnReviewn Never print password into logn Interfaces and methodsn Variable Namesn Unused classes and methods(especially the unused test classes)n Is Exception handling done correctly in HotReload and Services?nnnnTOODnRead OCP, SOLID, Liskov https://softwareengineering.stackexchange.com/questions/178488/lsp-vs-ocp-liskov-substitution-vs-open-closenJava streams are Lazily processed. Is it ture?nWhy static class?nMaven project vs module?
